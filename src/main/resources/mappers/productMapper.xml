<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.modernhome.mapper.ProductMapper">
	
	<!-- 완제품 목록 조회 -->
	<select id="productList" resultType="com.modernhome.domain.ProductVO">
		select *
		from product
	</select>
	<!-- 완제품 목록 조회 -->
	
	<!-- 완제품 검색 결과 조회 -->
	<select id="proSearchList" resultType="com.modernhome.domain.ProductVO"
		parameterType="java.util.Map">
		select * from product
		where
		<choose>
			<when test="itemOption == 'name'">
				pro_name like CONCAT('%', #{search}, '%')
			</when>
			
			<otherwise>
				pro_num like CONCAT('%', #{search}, '%')
			</otherwise>
		</choose>
	</select>	
	<!-- 완제품 검색 결과 조회 -->
	
	<!-- 완제품 등록 -->
	<insert id="regProduct">
		insert into product (pro_num, pro_name, pro_unit, pro_price)
		SELECT CONCAT(SUBSTRING(pro_num, 1, 2), LPAD(max(SUBSTRING(pro_num, 3)) + 1, 6, '0')), #{pro_name}, #{pro_unit}, #{pro_price}
		FROM product
		WHERE pro_num LIKE CONCAT('%', 'PR', '%')
	</insert>
	<!-- 완제품 등록 -->
	
	<!-- 완제품 목록 (팝업) -->
	<select id="getPopUpPro" resultType="ProductVO">
		select pro_id, pro_num, pro_name
		from product
	</select>
	<!-- 완제품 목록 (팝업) -->
	
	
</mapper>